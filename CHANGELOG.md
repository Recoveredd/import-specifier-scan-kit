# Changelog

## 0.1.1

- Return structured diagnostics for non-string runtime input.
- Report invalid `maxLength` options while falling back to the default scan limit.

## 0.1.0

- Initial release with static import, export-from, dynamic import, and literal `require()` scanning.
- Add package-name helpers for dependency previews and audits.
